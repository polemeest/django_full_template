from django.urls import path

# from apps.chat.consumers import ChatConsumer, OnlineStatusConsumer, ConversationsConsumer

websocket_urlpatterns = [
    # path("api/v1/chats/<conversation_name>/", ChatConsumer.as_asgi()),
    # path("api/v1/conversations_sidebar/", ConversationsConsumer.as_asgi()),
    # path("api/v1/online_check/", OnlineStatusConsumer.as_asgi()),
]